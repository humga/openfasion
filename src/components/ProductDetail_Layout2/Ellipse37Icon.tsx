import { memo, SVGProps } from 'react';

const Ellipse37Icon = (props: SVGProps<SVGSVGElement>) => (
  <svg preserveAspectRatio='none' viewBox='0 0 36 36' fill='none' xmlns='http://www.w3.org/2000/svg' {...props}>
    <circle opacity={0.35} cx={18} cy={18} r={18} fill='black' />
  </svg>
);

const Memo = memo(Ellipse37Icon);
export { Memo as Ellipse37Icon };
